// H1 CrystalsCollector

//Text: You will be given a random number at the start of the game.
//There are four crystals below. By clicking on a crystal you will add a specific amount of points to your total score.a
//You win the game by matching your total score to a random number, you lost the game if your total score goes above the random number.
//The value of each crystal is hidden from you until you click on it.
//Each time when the game starts, the game will change the values of each crystal

$(document).ready(function() {

//At the start of the game there is a randomly generated number (between 19 - 120)
function getRandomInt(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
var random = getRandomInt(19, 120)


$ ('#startingNumber').text(random);

//4 crystals, each represents a different number/value which is unknown value of crystals is randomly generated by computer between 1-12

var redCrystal=Math.floor((Math.random() * 12) + 1);
var blueCrystal=Math.floor((Math.random() * 12) + 1);
var yellowCrystal=Math.floor((Math.random() * 12) + 1);
var greenCrystal=Math.floor((Math.random() * 12) + 1);

//Each time you pick a crystal, you add the crystal worth to your score

//Shows total score on the bottom

// you want to match your total score to the generated number

//Define variables for wins and losses
var userScore = 0;
var wins = 0;
var losses = 0;

//Win: 0 section

//Loss: 0 section

//When you win you get 1 point, and alert that says you won! Win=matching computer number
function win () {
    alert ("You Won!");
}

//When you lose you get 1 loss, and alert that says you lost! Loss=going over given number

//When you win or lose, game resets with a new generated number


});   
